<%= form_for @email, :class => "center" do |f| %>
    <div class="form-group">
      <%= f.label :email %></br>
      <%= f.text_field :email %>
    </div>
     <div class="form-group">
     <%= f.submit "Join", :class => "btn btn-info btn-lg" %>
    </div>
    <% end %>
</div>
